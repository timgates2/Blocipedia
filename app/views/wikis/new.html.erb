<h1>New Wiki</h1>

<div class="row">
  <div class="col=md-4">
    <p>Guidelines for Wikis</>
      <ul>
          <li>Make sure it is grammatically correct.</>
          <li>Do not repeat yourself.</li>
          <li>Remember, TLDR is no bueno!</li>
      </ul>
  </div>
  <div class="col=md-8">
    <%= form_for @wikis do |f| %>
      <div class="form-group">
           <%= f.label :title %>
           <%= f.text_field :title, class: 'form-control', placeholder: "Enter Wiki title" %>
         </div>
         <div class="form-group">
           <%= f.label :body %>
           <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter Wiki body" %>
         </div>
         <div class="form-group">
           <%= f.submit "Save", class: 'btn btn-success' %>
         </div>
       <% end %>
     </div>
   </div>
